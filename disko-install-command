nix run github:nix-community/disko -- --mode disko ./disko.nix
zfs mount -a
mount -t vfat /dev/disk/by-partlabel/ESP /mnt/boot
# Or use `/dev/disk/by-uuid/...` depending on your naming
# Check: Run lsblk -f or findmnt to verify everything is mounted under /mnt.
